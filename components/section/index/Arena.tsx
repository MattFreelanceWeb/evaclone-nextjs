import React from 'react'

type Props = {}

function Arena({}: Props) {
  return (
    <section className="w-screen h-screen flex items-center justify-center relative">Arena</section>
  )
}

export default Arena