import React from 'react'

type Props = {}

function Bar({}: Props) {
  return (
    <section className="w-screen h-screen flex items-center justify-center relative">Bar</section>
  )
}

export default Bar